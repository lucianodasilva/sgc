#include <iostream>
#include <string>

#include <sgc.h>
#include <sgc/vector.h>
#include <sgc/map.h>

/*
struct test_struct;

struct other_struct {
	sgc::gc_ptr < test_struct > ptr;

	~other_struct () {
		std::cout << "other killed" << std::endl;
	}
};

struct test_struct {
public:

	sgc::vector < sgc::gc_ptr < other_struct > > test_vector;
	sgc::map < int, sgc::gc_ptr < other_struct > > test_map;

	~test_struct () {
		std::cout << "test killed" << std::endl;
	}
};
 */

int main (int arg_c, char ** arg_v) {
	return 0;
}

